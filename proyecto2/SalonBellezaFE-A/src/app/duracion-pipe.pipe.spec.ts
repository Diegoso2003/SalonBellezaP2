import { DuracionPipePipe } from './duracion-pipe.pipe';

describe('DuracionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DuracionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
